import {environment} from "../../../../environments/environment";

export class Images {
  static block1 = `${environment.imagesPath}/block1.png`;
  static roadVertical = `${environment.imagesPath}/road-vertical.png`;
  static roadHorizontal = `${environment.imagesPath}/road-horizontal.png`;
  static roadMerge = `${environment.imagesPath}/road-merge.png`;
}
